DELETE an existing instance of a Task
---
definitions:
  Task:
    required:
        - project_id
        - info
        - n_answers
        - long_description
        - owner_id
    properties:
      project_id:
        description: Project ID
        type: integer
        example: 1
      info:
        description: Task information
        type: object
        example:
          question: Who am I?
          user: HAL
      n_answers:
        description: Number of answers expected
        type: integer
        maxLength: 255
        example: 30
tags:
  - Tasks
security:
  - APIKeyQueryParam: []
parameters:
  - name: oid
    in: path
    required: true
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/definitions/Task'
responses:
  200:
    description: The task that was deleted
    schema:
      $ref: '#/definitons/Task'
