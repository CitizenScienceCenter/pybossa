POST to create a task
---
security:
  - APIKeyQueryParam: []
parameters:
  - name: task
    in: body
    type: object
    required: true
    schema:
      $ref: '#/definitons/Task'
produces:
  - application/json
responses:
  200:
    description: The task that was created
    schema:
      $ref: '#/definitons/Task'
  401:
    description: Access Denied