Change an existing isntance of a User
---
tags:
  - Users
parameters:
  - name: oid
    in: path
    required: true
requestBody:
  required: true
  content:
    multipart/form-data:
      schema:
        type: json
responses:
  200:
    description: The user that was changed
