GET all users
---
tags:
  - Users
parameters:
  - name: oid
    in: path
    type: integer
    required: false
produces:
  - application/json
responses:
  200:
    description: A list of users (or one matching the provided ID)