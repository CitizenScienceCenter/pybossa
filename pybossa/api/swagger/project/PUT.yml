Change an existing instance of Project
---
tags:
  - Projects
parameters:
  - name: oid
    in: path
    required: true
    type: integer
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/definitions/Project'
responses:
  200:
    description: The project that was changed
    schema:
      $ref: '#/definitions/Project'
